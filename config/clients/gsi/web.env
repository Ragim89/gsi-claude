VITE_APP_TITLE=GSI ERP
VITE_APP_SHORT_NAME=GSI ERP
VITE_THEME_COLOR="#105098"
# Optional: Dashboard hero background photo (inspection/port/vessel), per client profile.
# Unset here on purpose — no real photo asset ships in this repo, so the Dashboard falls back
# to its built-in abstract port illustration. Point this at a hosted image URL to override it.
#VITE_DASHBOARD_HERO_IMAGE=
