# CHANGELOG

Формат: обратный хронологический порядок, по фазам из [`IMPLEMENTATION_PLAN.md`](IMPLEMENTATION_PLAN.md).

## PHASE 12 — Безопасность, тесты, производительность · 2026-09-26

### Добавлено
- **Refresh-token registry** (`apps/api/migrations/025_refresh_tokens.sql`): таблица `refresh_tokens`, RLS без политик — доступ только через четыре `SECURITY DEFINER`-функции (`auth_issue_refresh_token`, `auth_rotate_refresh_token`, `auth_revoke_refresh_token`, `auth_revoke_all_refresh_tokens`), тот же приём, что `doc_counters`/`auth_find_user`. Refresh-токен теперь opaque-строка (32 случайных байта), в базе хранится только её SHA-256; сам токен никогда не попадает в JS — только в httpOnly cookie `gsi_rt`, `Path=/api/auth`, `Secure` в production.
- **Ротация с защитой от повтора**: `auth_rotate_refresh_token` — один атомарный SQL-вызов с `FOR UPDATE`: находит токен, при повторном предъявлении уже использованного (или отозванного) токена сжигает **всю семью** токенов той же сессии, а не только предъявленную копию. При деактивации пользователя (`is_active = false`) следующий refresh отклоняется тем же вызовом — без отдельного хука в `users.service`.
- **Logout / logout-all**: `POST /api/auth/logout` (доступен без access-токена — выход не должен требовать то, что он же аннулирует) и `POST /api/auth/logout-all` («выйти на всех устройствах», кнопка в сайдбаре — `nav.logoutAll`). Оба пишут в `audit_logs` (`auth.logout`, `auth.logout_all`), реиспользование токена — отдельным, более заметным `auth.refresh.reuse_detected`.
- **Индексы под реальные запросы** (`apps/api/migrations/026_analytics_turnaround_indexes.sql`): `inspections(job_id)` и `reports(job_id)` — полные (не частичные) индексы. `EXPLAIN ANALYZE` на `GET /analytics/turnaround` показал последовательное сканирование обеих таблиц 999 раз (по разу на заявку), хотя частичные индексы `WHERE deleted_at IS NULL` уже существовали: RLS фильтрует строки функцией `app_is_live(deleted_at)`, а не буквальным `deleted_at IS NULL`, и планировщик не может доказать, что первое подразумевает второе — поэтому частичный индекс никогда не выбирался ни для одного RLS-защищённого запроса к этим таблицам. Полный индекс работает под фильтром RLS, а не пытается совпасть с ним. Измеренный эффект на сидовых данных: 90 мс → 27 мс для этого запроса.
- **Нагрузочный скрипт** `scripts/load-test.mjs`: Jobs (список и поиск), Analytics (jobs, turnaround), Search, Audit, Finance dashboard, Reports — по 100 запросов (20 параллельных × 5 раундов) на каждый, p50/p95/max/error rate.
- Локали `nav.logoutAll` (en/ru/tr).

### Сознательно не сделано
Индекс на `samples(job_id)` не добавлен: `EXPLAIN ANALYZE` после пробного добавления показал, что планировщик всё равно выбирает последовательное сканирование — таблица (~54 строки на сидовых данных) для этого слишком мала, индекс был бы мёртвым весом. Общий rate-limit (300 запросов в минуту на пользователя) не менялся: нагрузочный скрипт, наоборот, подтвердил его исправную работу — быстрый прогон всех целей от одного пользователя закономерно ловит `429`.

### Проверено
`verify.sh`: 103 unit + 308 integration = 411 тестов (было 403; +8 в `auth.spec.ts` — ротация и повтор, logout, logout-all с двух сеансов, отказ деактивированному пользователю), lint/typecheck/API build/web build зелёные. `verify-full.sh`: 90 smoke-проверок без изменений, зелёный на пересобранном стенде с применёнными миграциями 025/026. Дополнительно вручную на живом стенде: httpOnly-cookie видна в `Set-Cookie` с `HttpOnly`/`Path=/api/auth`; повторное предъявление уже провёрнутого токена и его замена оба отклонены (401); logout аннулирует именно предъявленную сессию; logout-all с одного из двух устройств аннулирует оба (`{"revoked":11}` включая старые сессии от предыдущих ручных проверок); деактивация пользователя отклоняет его refresh немедленно, реактивация возвращает доступ; защищённый маршрут без токена — 401; `/api/public/verify` — 200 без авторизации, ложный код не подтверждает и не опровергает существование записи.

## PHASE 11 — PWA, мобильный интерфейс, офлайн, i18n · 2026-09-25

### Добавлено
- **Installable PWA**: `apps/web/public/manifest.webmanifest` (иконки 192/512 и маскируемая 512, из фирменного лого `logo-stacked.png`, сгенерированы через sharp — не выдуманы заново), `apple-touch-icon.png`, `theme-color`, ссылки в `index.html`. `apps/web/public/sw.js` — кэширует только статическую оболочку (`/`, `/index.html`, `/assets/*`, иконки) стратегией stale-while-revalidate; навигационные запросы — network-first с откатом на закэшированный `index.html`. Явно не перехватывает ни один запрос к `/api/*` — ни один ответ сервера не попадает в общий Cache Storage браузера. `InstallPrompt` перехватывает `beforeinstallprompt` и показывает баннер вместо того, чтобы полагаться на иконку в адресной строке.
- **Нижняя навигация** (`apps/web/src/components/Layout.tsx`) — только для `scope: own` (инспектор/лаборант): Мои задания / Мои инспекции / Мои пробы / Мои анализы, плюс «Ещё» открывает прежнее выдвижное меню PHASE 10.5. Текстом, без иконок — сайдбар уже отказался от них по той же причине (дизайн-система их не определяет). Десктопная и HQ-навигация не тронуты.
- **Офлайн-очередь на IndexedDB** (`apps/web/src/offline/{db,queue,OfflineProvider}.tsx`, не localStorage — фото хранятся как `Blob`): три вида операций — пакет ответов чек-листа, фото чек-листа, черновик пробы. Каждая операция помечена `userId` владельца; очередь и статус (`Offline/Queued/Syncing/Conflict/Failed`, топ-бар `OfflineBadge`) читаются только для активного пользователя — на одном устройстве очередь одного инспектора не видна и не синхронизируется под другим. Повтор — по событию `online` и таймеру с экспоненциальной паузой; неудачный сетевой запрос останавливает текущий проход, а не жжёт эфир телефона частыми попытками.
- **Офлайн-чтение чек-листа**: последний успешно загруженный чек-лист кэшируется в IndexedDB по `userId + inspectionId`; если сеть недоступна и запрос не может вернуть данные, экран показывает этот кэш вместо пустого списка — подтверждено на живом стенде (сеть отключена, чек-лист перечитан внутри той же сессии).
- **Конфликт 409** — тот же оптимистичный лок, что PHASE 3.8 завела для заявки, инспекции и результата лаборатории. Раньше он тонул в общей `ErrorBox`; теперь карточка инспекции (`InspectionDetailPage`) и результата лаборатории (`LabRequestDetailPage`) показывают отдельный `ConflictBanner` с объяснением и кнопкой «Обновить и посмотреть» — сервер не перезаписывается молча. Проверено намеренно вызванным конфликтом (два сеанса редактируют одну инспекцию).
- Ввод результата лаборатории теперь можно сохранить офлайн как черновик (тот же PATCH, что и онлайн, через очередь); submit/review/approve/release остаются только онлайн-действиями — карточка честно объясняет, почему.
- i18n: ключи `offline.*` и `pwa.*` добавлены en/ru/tr тем же способом, что и остальной интерфейс.

### Найденное и исправленное по пути
- `manifest.webmanifest` отдавался nginx как `application/octet-stream` — в базовом `mime.types` нет записи под `.webmanifest`, и без явного `default_type` Chrome тихо отказывается признавать файл веб-манифестом: страница работала, а «Установить» не предлагалось никак. Добавлен `location = /manifest.webmanifest { default_type application/manifest+json; }` в `apps/web/nginx.conf`.
- `icon-512.png`, сгенерированная скриптом на sharp, оказалась 513×513 — на пиксель больше заявленных в манифесте `512x512` из-за округления при паддинге под цветом бренда. Пересчитан паддинг так, чтобы `inner + 2×pad` давало точный размер без последующего масштабирования (которое сама по себе оказалось источником второго, более грубого расхождения при первой попытке чинить). Все пять иконок перегенерированы и сверены построчно с `file`.
- Реакция на офлайн у `useQuery` чек-листа опиралась на `isError`, но React Query по умолчанию (`networkMode: 'online'`) просто ставит запрос на паузу, когда браузер считает себя офлайн, и `isError` никогда не наступает — офлайн-кэш чек-листа молчал именно тогда, когда был нужнее всего. Запрос переведён на `networkMode: 'always'` (решение принимает наш собственный перехват сетевой ошибки, не менеджер React Query), а откат на кэш дополнительно триггерится по собственному состоянию `isOnline`, а не только по `isError`.

### Сознательно не сделано
Сервис-воркер не хранит `/api/*` вообще — это и есть защита от требования «не течь между пользователями», а не что-то, что предстоит доделать. Approve/release офлайн не реализованы намеренно: это моменты, когда результат становится официальным, и они не должны проходить без подтверждённой связи с сервером.

### Проверено
`verify.sh` и `verify-full.sh` зелёные (backend не тронут: 300 unit+integration тестов без изменений, 90 smoke-проверок без изменений). Вручную на живом стенде, после исправлений выше:
- **Манифест/иконки**: `Content-Type: application/manifest+json`, все пять PNG отдаются с размерами, точно равными заявленным в манифесте.
- **Офлайн-очередь чек-листа**: ответ и фото (реальный `Blob` в IndexedDB) поставлены в очередь офлайн, пережили перезапуск контейнера и полную перезагрузку страницы, автоматически синхронизировались при возврате сети — оба подтверждены на сервере (`result: "ok"`, фото появилось в `media` с объектом в MinIO).
- **Офлайн-чтение чек-листа**: при недоступной сети экран сразу показывает последний закэшированный список (после исправления `networkMode`), а не пустое состояние.
- **Конфликт 409**: воспроизведён намеренно на карточке инспекции (сервер получает более новую версию, пока форма ещё открыта со старой) — баннер показывается, «Обновить и посмотреть» корректно подтягивает актуальную версию.
- **Изоляция очереди по пользователю**: супервайзер поставил правку в очередь офлайн → вышел из системы → вошёл другой инспектор — бейдж не показывает чужую единицу в очереди, а сам элемент остаётся в IndexedDB нетронутым; после повторного входа супервайзера очередь на месте и синхронизируется под его же сессией. Проверено полным циклом logout/login через интерфейс, не только переключением активного пользователя в памяти.
Миграция не потребовалась — изменения только в `apps/web`.

## PHASE 10 — Документы и уведомления · 2026-09-25

### Добавлено
- **Общий реестр документов** (`documents`, миграция 023) — отдельно от документов-отчётов PHASE 7: строка либо хранит свой файл (PDF/Word/Excel/изображение, MIME и расширение проверяются друг против друга), либо ссылается на уже выпущенный отчёт (`report_version_id`) без второй копии PDF — `ReportDocumentsService.issue()`/`issueForJob()` регистрируют такую ссылку сразу после выпуска, в той же транзакции. Загрузка/просмотр/архивирование — `POST/GET /documents`, `POST /documents/:id/archive`, три новых права. Прикреплено к карточкам клиента, заявки, инспекции, пробы.
- **Центр уведомлений** (`notifications`, миграция 024) — колокольчик в интерфейсе, live-обновление через SSE (`GET /notifications/stream`, по образцу `/finance/stream`). События приходят из уже существующей шины `JobEventsService` (сеть, которую PHASE 3 явно оставила как «шов для будущих уведомлений»): назначение заявки, приём пробы лабораторией, результат/отчёт на рассмотрении или утверждении, выпуск отчёта. Два типа не привязаны к действию — инспекция скоро и просроченный счёт — их находит часовой сдвиг (`notifications-cron.service.ts`) с дедупликацией.
- **Почтовый адаптер** (`email/`), провайдер-нейтральный: `console` (по умолчанию, только лог) или `smtp` (любой провайдер по SMTP). Подключён к `RemindersService` — напоминание о просрочке теперь и пишется в лог, и (если у клиента есть основной контакт с адресом) уходит письмом.
- **Глобальный поиск** (`GET /search?q=…`) — заявки, клиенты, пробы, отчёты/сертификаты, счета по номеру или названию. Не заводит отдельной модели прав: каждый под-запрос — тот же `SELECT`, что уже выполняет список соответствующей сущности, под теми же RLS-политиками (включая `finance.read` на счетах).
- 5 новых прав (`document.read/upload/archive`, `notification.read`, `search.read`), 132 всего.
- Регрессия: 3 новых проверки в `scripts/smoke.mjs` (документы заявки, список уведомлений, поиск) — 90 вместо 87.
- Тестов стало 403 (103 unit — без изменений, 300 интеграционных — 10 новых в `documents-notifications-search.spec.ts`).

### Найденное и исправленное по пути
`DocumentRegistryService.archive()` первой версией падал с `42501 new row violates row-level security policy`, хотя и право, и область видимости были верны. Причина — в Postgres: когда `SELECT` и `UPDATE` заведены как отдельные политики (у `documents` так и есть, ради разных прав на чтение/загрузку/архивирование), результат `UPDATE` дополнительно проверяется по `USING` политики `SELECT`, а не только по `WITH CHECK` своей собственной — а `documents_read` требует `app_is_live(archived_at)`, которое сама операция архивирования и делает ложным. Подтверждено экспериментально на минимальной копии таблицы (см. `docs/DATABASE.md`). Решение — то самое переключение `{ includeArchived: true }`, которое `db.service.ts` и `010_soft_delete.sql` уже документируют как нужное для восстановления архивной записи: оно нужно и для того, чтобы её архивировать.

### Сохранено без изменений
Report/Certificate document engine (PHASE 7), QR/PDF-логика, `StorageService`, Finance SSE и `RemindersService`'s собственный лог остались нетронуты — PHASE 10 подключилась к ним, а не заменила. `JobEventsService` физически переехала в `CoreModule` (была в `OperationsModule`), чтобы `DocumentsModule` могла эмитить события без цикла модулей — сигнатура и поведение те же, только область видимости провайдера. Working-stack smoke — 90 проверок, 0 упавших; `verify.sh`/`verify-full.sh` — зелёные.

## PHASE 9 — Аналитика · 2026-09-25

### Добавлено
- **Отдельный модуль `analytics`**, не расширение финансового дашборда: `AnalyticsController` / `AnalyticsService` со своими правами, читающие уже существующие `inspection_jobs`, `inspections`, `samples`, `test_requests`, `test_results`, `report_versions`, `job_assignments` — новых таблиц нет.
- **Заявки**: по месяцам, офису, стране, клиенту (топ-10), услуге, статусу — `GET /analytics/jobs`.
- **Сроки прохождения (turnaround)**: заявка → инспекция, инспекция → лаборатория, лаборатория → выпущенный результат, выпущенный результат → отчёт, и заявка → отчёт целиком — среднее, медиана, P90 и число заявок, попавших в выборку (стадия без данных честно отвечает `null`, а не нулём). Тренд заявка→отчёт по месяцу выпуска — `GET /analytics/turnaround`.
- **Загрузка**: инспекторы и пробоотборщики — из `job_assignments`; лаборанты и лаборатории — из `test_requests`/`test_results`; рецензенты — из `report_versions.reviewed_by`. У каждого — «в работе» (открыто прямо сейчас) и «завершено» (закончено в выбранном периоде) — `GET /analytics/workload`, отдельное право `analytics.workload`.
- **Executive dashboard — та же страница для всех.** Отдельных экранов «для HQ» и «для инспектора» не заведено: Row-Level Security, та же, что у любого другого списка, превращает `GET /analytics/jobs` в «группа» для HQ и в «мои заявки» для инспектора с областью `own` — проверено напрямую (администратор видит 999 заявок, инспектор тем же вызовом — свои 162).
- 2 новых права (`analytics.read`, `analytics.workload`), 127 всего.
- Экран `AnalyticsPage`: переиспользует компоненты графиков дашборда (`ChartFrame`, `Columns`, `BarList`, `StatTile`) — ни одного нового способа рисовать график не введено. en/ru/tr, проверено на 390×844, 768×1024 и десктопе — горизонтальной прокрутки нет.
- Регрессия: 3 проверки (`analytics/jobs`, `/turnaround`, `/workload`) добавлены в `scripts/smoke.mjs`, 87 проверок вместо 84.

### Производительность
- `EXPLAIN ANALYZE` на посевных данных (999 заявок) нашёл два реальных повода, не гипотетических: у `reports` не было индекса по `job_id` (есть по всем остальным внешним ключам) — добавлен `reports_job_idx`; CTE turnaround без `MATERIALIZED` инлайнилась и пересчитывала `report_issued` дважды на каждую заявку — добавлен `MATERIALIZED`, а три отдельных подзапроса к `samples`/`test_requests`/`test_results` слиты в один `LATERAL`-проход. Вместе — с ~220 мс до ~90 мс на том же запросе и тех же данных. Новых индексов сверх `reports_job_idx` не потребовалось: существующие (`inspections_job_idx`, `samples_job_idx`, `test_requests_sample_idx`, `report_versions_report_idx`) уже покрывали остальные соединения.

### Сохранено без изменений
Финансовый дашборд (`/finance/dashboard`) не тронут ни строкой — аналитика не расширяет его, а стоит рядом, как и просил план. 393 существующих теста (103 unit, 290 интеграционных) прошли без единой правки под них.

## PHASE 8 — Финансы · 2026-09-25

### Добавлено
- **Услуги и цены.** `services` — каталог того, что продаётся (как `commodities`/`ports`, не привязан к офису). `prices` разрешаются в порядке контракт → клиент → офис по умолчанию — та же логика, `app_resolve_price`, что и у лабораторных норм в PHASE 6.
- **Предложение со своим жизненным циклом.** `draft → sent → accepted | rejected | expired`, плюс `cancelled`, и `revise` возвращает в `draft` с обязательной причиной — предложение можно послать заново, в отличие от отчёта у него нет ревизий и снимков. `QuoteWorkflowService` — той же формы, что у заявки, инспекции, пробы, анализа и документа. Принятое предложение превращается в счёт: `POST /finance/quotes/:id/create-invoice` копирует его позиции через тот же `InvoicesService.create`, которым пользуется обычное создание счёта.
- **Платёж стал сущностью, а не побочным эффектом.** `payments` + `payment_allocations`: один платёж можно разнести на несколько счетов, а можно оставить частично или полностью неразнесённым — аванс или переплата, — и разнести позже. Деньги, которых пока не к чему применить, всё равно немедленно попадают в проводку, только на клиринговый счёт (`ar.unapplied`), а не на конкретный счёт клиента. `InvoicesService.pay()` не изменился ни в сигнатуре, ни в проводке — он стал частным случаем `PaymentsService.create()` с одной, полностью разнесённой позицией.
- **Кредиторская задолженность по расходам.** Расход по умолчанию по-прежнему считается оплаченным сразу — ничего не изменилось для того, что уже было. Флаг `onAccount: true` книгует его как обязательство (`expense` / `ap.trade`), а не как деньги, ушедшие из кассы; оплатить его позже — исходящий платёж (`expense.pay`) с той же логикой частичной оплаты, что и у счёта клиента.
- **Себестоимость и маржа заявки.** `GET /finance/jobs/:id/summary` — выручка и затраты это счета и расходы, у которых `job_id` уже указывает на заявку с самой первой финансовой миграции; отдельной таблицы затрат не заведено — плодить вторую запись того же значило бы дублировать `expenses`. Каждая строка пересчитана по курсу своей даты (`fx_rate_on`), как и всё остальное в этом модуле — «многовалютная маржа» и означает то же самое, что уже означало для дашборда. Право `job.read_finance`, зарезервированное для этого ещё в PHASE 3, наконец использовано.
- **Акт сверки клиента.** `GET /finance/clients/:id/statement` — бегущее сальдо: выставленный счёт дебетует, применённый платёж кредитует, каждая сумма — по курсу своей даты.
- **Напоминание о просрочке — лог, а не письмо.** Адаптера e-mail в системе ещё нет (это PHASE 10); `invoice_reminders` только записывает, кто, когда и что сказал клиенту. Кнопка называется «Записать напоминание», а не «Отправить», — это тот шов, в который PHASE 10 вставит настоящую отправку, а не имитация одной.
- **Финансовый дашборд** получил воронку предложений по статусам, сумму кредиторской задолженности и сумму неразнесённых платежей — аддитивно, поверх того, что уже считал.
- 17 новых прав (`service.*`, `pricing.*`, `quote.*`, `payment.*`, `expense.pay`, `invoice.remind`), 125 всего. `pricing.*`/`quote.*` изолированы как коммерческие данные — та же граница, что у `contract.*` с PHASE 2; `service.*` открыт всем, у кого есть `reference.read`, — каталог того, что продаётся, не тайна.
- Экраны: реестр и карточка предложения, реестр платежей с формой регистрации входящего/исходящего платежа и разнесения остатка, каталог услуг и цен, вкладка «Revenue / Costs / Margin» на карточке заявки, вкладка «Акт сверки» на карточке клиента, карточка напоминаний на счёте, три новых виджета дашборда.
- 22 новых теста (`test/finance.spec.ts`) — **первый интеграционный тест для модуля финансов**, работавшего без единого автоматического теста с MVP-1: старое поведение `pay()` зафиксировано наравне с новым (разнесение на несколько счетов, переплата и её последующее разнесение, кредиторская задолженность, маржа заявки, акт сверки, изоляция коммерческих данных), плюс отдельная проверка `SUM(debit) = SUM(credit)` по всей таблице проводок после всего, что сделала фаза. Тестов стало 393 (103 unit, 290 интеграционных).

### Исправлено при разработке
- `applyAllocation` писал строку `payment_allocations` до того, как проверял, сколько ещё причитается, — вторая частичная оплата одного и того же расхода отказывала с «уже оплачено», потому что запрос считал только что вставленную строку дважды. Проверка теперь идёт первой, запись — последней.
- `ClientStatementService` сравнивал даты как строки, не приведя колонку `date` через `to_char`: `pg` возвращает её объектом `Date`, и его `toString()` лексикографически больше любой даты вида `YYYY-MM-DD`, из-за чего каждая строка попадала мимо периода и акт сверки всегда показывал нулевое сальдо.
- **Найден и исправлен предсуществующий дефект**, обнаруженный визуальной проверкой вкладки «Финансы» заявки: `jobId` не входил в белый список `InvoiceQueryDto`, и `GET /finance/invoices?jobId=…` отвечал 400 с MVP-1 — вкладка заявки с её списком счетов не работала никогда. Права `jobId` в DTO и в `InvoicesService.list()` не было ни в одном из предыдущих фаз.

### Сохранено без изменений
Существующая проводка `pay()` (`cash.bank` / `ar.trade`, `source_type: 'payment'`) не тронута — `GET /finance/invoices/:id/payments` по-прежнему её читает для счетов, история платежей которых не касалась `PaymentsService`. 268 существующих интеграционных тестов прошли без единой правки под них.

## PHASE 7 — Отчёты и сертификаты · 2026-09-25

### Добавлено
- **Документ со своей жизнью.** Отчёт больше не появляется только в момент утверждения заявки: его начинают, пишут, сдают на проверку, возвращают с причиной, утверждают и выпускают. `ReportWorkflowService` — пятый экземпляр того же движка, что у заявки, инспекции, пробы и анализа, и единственное место, которое пишет статус документа.
- **Выпущенный документ перестаёт читать базу.** Всё, что он утверждает, копируется при выпуске в `report_versions.data_snapshot`: клиент, заявка, инспекции с чек-листом, замечаниями и замерами, пробы, выпущенные результаты с методиками, нормами и единицами, подписи. PDF рендерится из этой копии, хешируется (SHA-256) и сохраняется один раз — скачивание через год отдаёт те же байты. Переименованный клиент или пересмотренная методика не могут изменить сертификат, подписанный сегодня.
- **Ревизия вместо правки.** Выпущенный документ не редактируется: `revise` с обязательной причиной открывает новую ревизию, которая начинается с того, что было выпущено, а выпущенная копия остаётся ровно такой, какой её отправили, — со своим файлом, своей контрольной суммой и **своим QR-кодом**. Один код на документ не смог бы отличить копию, которую человек держит в руках, от более поздней.
- **Публичная проверка говорит правду о неудобных случаях.** Заменённая ревизия отвечает «действительный исторический документ, заменён ревизией N», отменённый документ отвечает «отменён» с причиной. Ответить 404 значило бы научить людей не доверять проверке. Ответ при этом стал тоньше: номер, тип, ревизия, дата, офис-эмитент и контрольная сумма — **имени клиента там больше нет**.
- **Только выпущенные результаты лаборатории.** В документ попадает `released` и ничего другого: внесённый, проверенный и даже утверждённый результат — ещё не то, что можно показать клиенту. Правило живёт в `ReportDataService`, а не на экране, который мог бы о нём забыть.
- **Семь типов документов из одного рендерера.** Шаблон — это список секций и их настроек в `report_templates`, а не React-компонент: акт инспекции, сюрвейерский отчёт, протокол испытаний, сертификат анализа, сертификат, акт отбора проб и произвольный отчёт печатает один код. Версию формы поднимает триггер базы, и каждая выпущенная ревизия помнит, какой версией её напечатали.
- **Точное представление числа — хвост PHASE 6.** `test_results.numeric_text` хранит значение ровно так, как его набрал аналитик: 12.40 печатается как 12.40, а не как 12.4. Число остаётся `numeric` для вычислений; сертификат печатает текст. Ввод с числом знаков, которого колонка не удержит, теперь отклоняется с внятным сообщением, а не округляется молча.
- **Сертификаты получили свою последовательность номеров** (`TR-C-2026-00001`) через тот же `next_doc_number`; формат `TR-R-…`, напечатанный на 520 существующих отчётах, не тронут.
- **Межофисный read model наименьших привилегий** (`lab_sample_brief`): лаборатория в чужом офисе видит номер пробы, культуру, количество, пломбу, инструкции и имя клиента — и ничего из контрактов, тарифов и счетов. Это ответ на ограничение, найденное в PHASE 6, и он принят явно, а не получился сам собой из JOIN.
- Экраны: реестр документов с пагинацией, поиском и фильтрами; мастер создания (тип → язык → заявка → что в ней есть → текст); карточка документа с вкладками «Детали», «Содержание», «Источники данных», «Ревизии» и «История»; предпросмотр черновика с водяным знаком; выбор фотографий. Язык документа отделён от языка интерфейса: сертификат можно написать по-английски, сидя в русском интерфейсе.
- 12 прав `report.*`, выведенных из существующих. `report.self_approve` не входит ни в одну роль по умолчанию, включая администратора.
- 45 новых тестов: 15 на машину состояний документа и 30 сквозных и регрессионных — от снимка данных и точности 12.40 до проверки QR на заменённой и отменённой копии.

### Добавлено при финальной проверке фазы
- **Форма попала в снимок ревизии** (миграция 018, `report_versions.template_definition`). Ревизия записывала номер версии формы, но сама форма оставалась в `report_templates`, где её можно править: файл документа от этого не менялся, а вот запись «напечатан по форме v1» после перехода формы на v2 нельзя было проверить. Теперь можно.
- **Печатное представление числа согласовано с самим числом ограничением базы**: `numeric_text::numeric = numeric_value`. Сервис и раньше писал оба поля из одной проверенной строки; теперь они равны по правилу, а не только по построению.
- **26 тестов на четыре обещания по отдельности** (`test/reports-integrity.spec.ts`): только выпущенный результат цитируется — по одному тесту на «внесён», «сдан», «проверен», «утверждён», «выпущен»; выпущенный документ не меняется — контрольная сумма скачанного файла, переименование клиента, правка заявки, смена формы, отказ в правке и в архивировании, своя сумма и свой код у каждой ревизии; напечатанное число равно измеренному — 12.40, 12.4, 0.001, 12345.6789 и отказ вместо тихого округления; лаборатория в чужом офисе не открывает ни контракт, ни счёт, ни адрес, ни контакты клиента.
- **Абзац, поданный под именем, которого рендерер не знает, теперь отклоняется** (400), а не сохраняется молча и не печатается никогда. Вложенный объект `content` валидировался только на вид: `@Type` без `@ValidateNested` — это объявление без проверки, и ограничение в 20 000 знаков тоже не действовало. Сюрвейер, написавший заключение, должен увидеть его на странице или узнать, почему нет.
- **Вид услуги печатается на языке документа**: русский сертификат больше не говорит «sampling» в графе «Услуга». Формулировки берутся из той же таблицы `SERVICE_TYPE_LABELS`, что и экраны, поэтому разойтись они не могут.
- **Три подписи стоят на одной высоте.** Дата есть только у утверждающего, и пустой `div` у двух остальных схлопывался, опуская их линии подписи ниже. Строка даты теперь занимает место всегда.
- Документация: описание межофисного brief в `WORKFLOWS.md` заменило устаревшее «известное ограничение» из PHASE 6.

### Изменено
- `GET /reports` стал страницей (`rows`/`total`/`limit`/`offset`) вместо массива с `LIMIT 1000` и получил поиск и фильтры. Карточки заявки и клиента читают ту же страницу.
- Документ, который выпускает утверждение заявки, теперь проходит через тот же снимок и тот же рендерер, что и все остальные: он больше не особый случай, просто за одно решение не просят расписаться дважды.
- Визуальная проверка PDF нашла две ошибки, которых не видно по коду 200: переменные темы оборачивались в `:root` дважды (документ печатался чёрно-белым шрифтом с засечками), а логотип подставлялся функцией вместо изображения. Обе исправлены; сертификат печатается на фирменном бланке.

### Сохранено без изменений
**520 выпущенных отчётов сохранили номера, файлы, контрольные суммы и QR-коды** и продолжают проверяться и скачиваться. Снимков данных им не выдумывали: чего никто не записывал в прошлом году, того сегодня не восстановить, и такие ревизии честно помечены `is_legacy`.

Миграции 016 и 017 применены к рабочей базе: 967 заявок, 968 инспекций, 20 проб, 73 события цепочки хранения, 520 отчётов, 942 счёта, 8 медиафайлов и 29 результатов анализов — до и после совпадают до строки. CRM, заявки, инспекции, чек-листы, замечания, замеры, фотографии, пробы, цепочка хранения, лаборатория, отчёты, QR, финансы, активы, аудит, импорт и экспорт проверены регрессией на работающем стенде.

## PHASE 6 — Лаборатория / LIMS · 2026-09-24

### Добавлено
- **Анализ как единица работы лаборатории** (`test_requests`): один анализ одной пробы, со своей методикой, нормой, исполнителем, сроком и жизненным циклом. Не «лабораторная работа по заявке» и не «протокол»: влажность и белок идут по разным методикам, на разном оборудовании, у разных людей и заканчиваются в разное время.
- **Жизненный цикл из 10 состояний**: запрошен → назначен → выполняется → результат внесён → на проверке → утверждён → выпущен, плюс приостановлен, отклонён и отменён. `LabWorkflowService` — четвёртый экземпляр той же формы движка, что у заявки, инспекции и пробы, и единственное место, которое пишет статус.
- **Три подписи трёх решений.** Тот, кто измерил, не проверяет себя; проверка — это подпись, а не смена статуса; утверждение — отдельное право. Попытка проверить или утвердить собственный результат отклоняется. Исключение существует и оно именное: право `lab.result.self_approve` для лаборатории, где физически работает один человек, — оно не входит **ни в одну** роль по умолчанию, включая администратора.
- **Утверждено — ещё не выпущено.** `approved` значит «число верное», `released` — «его можно показывать наружу». Отчёты читают лабораторию только через `GET /lab/released`, и утверждённое, но не выпущенное туда не попадает.
- **Ревизии вместо правки.** Утверждённый результат не редактируется: `amend` с обязательной причиной создаёт новую ревизию и возвращает анализ на рабочее место, а предыдущая остаётся ровно такой, какой её подписали, — потому что она уже процитирована в отчёте, ушедшем клиенту. Частичный уникальный индекс не даёт существовать двум действующим ревизиям.
- **Каталог анализов построен из того, что уже было.** 34 анализа взяты из `commodities.lab_methods` — списка, который лежал в справочнике культур с миграции 004. Отдельной таблицы «наборов» нет: набор по культуре — это тот же список, и вторая его копия была бы вторым списком, который надо держать в согласии с первым.
- **Методики с версией, которую поднимает база.** Правка предела, стандарта, единицы или области аккредитации поднимает `version` триггером; правка описания — нет. В результат методика копируется снимком, поэтому «по какой версии это измеряли» остаётся ответом навсегда, а не превращается в «по последней».
- **Нормы с порядком разрешения в SQL** (`app_resolve_specification`): контракт → клиент → культура → норма без привязки, при равенстве — действующая с более поздней даты. Порядок задан в базе, а не в приложении: тогда и запрос анализа, и любой будущий отчёт видят одну и ту же норму. Найденная норма тоже копируется в результат снимком.
- **Точность как требование, а не как повезёт.** Численный результат идёт от формы до колонки `numeric(18,6)` **строкой**, ни разу не превращаясь в `double precision`: 0.001 и 12345.6789 возвращаются из базы ровно такими, какими их ввели, без артефактов двоичной плавающей точки. Колонка хранит точное десятичное значение; число значащих цифр, набранное аналитиком (12.40 против 12.4), она не различает — это вопрос оформления сертификата и решается в PHASE 7.
- **Выход за норму показывается, а не исправляется.** Значение остаётся ровно таким, каким его измерили; статус получает явную отметку `out_of_spec`, запись попадает в аудит и в отдельный фильтр очереди. Что делать с отклонением, решают клиент и лаборатория.
- **Приборы и поверка** (`lab_instruments`). Просроченная поверка — предупреждение, а не запрет: отказать в записи уже сделанного измерения значит потерять измерение, а прибор с просроченной поверкой у лаборатории всё равно останется. Факт просрочки записывается в результат и остаётся там навсегда.
- **История анализа только на добавление** (`test_request_status_history`), теми же грантами, что у истории и цепочки проб: у `gsi_app` есть `SELECT` и `INSERT` и нет `UPDATE`/`DELETE`.
- **Пять типов результата**: число, текст, «обнаружено / не обнаружено», качественный и «соответствует / не соответствует». Форма ввода следует типу анализа, а не наоборот.
- **Один анализ на пробу**: частичный уникальный индекс не даёт запросить ту же влажность по той же методике дважды — иначе в отчёте оказались бы два «правильных» значения. Отменённый и отклонённый запрос из индекса выпадают: после отказа лаборатории анализ можно попросить заново.
- Экраны: рабочая очередь с показателями, которые считаются по тем же строкам, что и список (клик по показателю — фильтр, а не другой экран); карточка анализа с вводом результата, проверкой, утверждением, выпуском, ревизиями и историей; каталог анализов с методиками; нормы; приборы; вкладка «Лаборатория» на карточке пробы и на карточке заявки. Всё на трёх языках.
- 17 прав `lab.*`, выведенных из существующих: кто видит пробы — видит анализы, кто отправляет пробы — запрашивает анализы. Лаборант получил рабочее место и не получил проверку с утверждением; руководитель лаборатории получил распределение, проверку, утверждение, выпуск и справочники и намеренно не получил `lab.result.enter`.
- Экспорт анализов и выпущенных результатов — тринадцатый и четырнадцатый разделы выгрузки; двенадцать существующих не тронуты. Результаты выгружаются только выпущенные.
- 69 новых тестов: 14 на машину состояний и 55 сквозных и регрессионных — от принятой пробы до выпущенного результата, включая точность десятичной дроби, отказ в самопроверке, отказ в правке утверждённого, сохранность заменённой ревизии и границы видимости между офисами.

- **Рабочие записи на ревизии**: распечатка прибора, запись взвешивания, рабочий журнал прикладываются к той ревизии, для которой их читали, и после сдачи результата уже не меняются. Колонка `media_attachments.test_result_id` из миграции 015 перестала быть заделом и получила эндпоинты и экран.

### Изменено
- Список анализов получил фильтр `reviewed`: «ждут проверки» и «ждут утверждения» — это два разных состояния внутри одного статуса `under_review`, и показатели на экране очереди теперь считают ровно те строки, которые она покажет по клику.
- Показатели лаборатории принимают те же фильтры, что и список (`laboratoryId`, `branchId`, `mine`): у лаборанта, открывшего «мои анализы», плитки считают его рабочее место, а не работу всей группы.
- Демо-данные: два лаборанта в Стамбуле, 18 методик со стандартами (ISO, ICC, GAFTA, AOAC), 11 норм по культурам и 5 приборов, один из которых намеренно с просроченной поверкой — предупреждение на экране ввода результата должно быть видно в демонстрации.
- Попутно исправлены три подписи в интерфейсе, которые показывали пользователю ключ перевода вместо текста (`invoices.status`, `import.commit`, `jobs.selectInspector`). Проверка теперь охватывает все 774 ключа в трёх языках.

### Известное ограничение
Проба, отправленная в лабораторию **другого** офиса, видна принимающему офису по политике, но открыть её карточку он не может: и проба, и анализ читаются с `JOIN` к заявке и клиенту, которые принадлежат офису-отправителю. Межофисную отправку сегодня доводит до конца офис-владелец заявки. Открыть другому офису коммерческие данные клиента — решение, а не исправление, поэтому оно вынесено отдельно и здесь не сделано.

### Сохранено без изменений
**Из существующих данных не создано ни одного результата анализа.** У замеров инспекций нет ни методики, ни исполнителя, ни подписи, а результат без них — это не результат, а число; выдумать методику ради заполненной таблицы значило бы подписать чужим именем. Лабораторные записи начинаются с первой заявки на анализ.

Миграция 015 применена к рабочей базе: 960 заявок, 961 инспекция, 5731 пункт чек-листа, 13 проб, 31 событие цепочки хранения, 520 отчётов, 942 счёта, 576 расходов, 61 актив и 4 лаборатории — до и после совпадают до строки. Заявки, инспекции, пробы, чек-листы, отчёты, QR-верификация, финансы, активы, загрузка и выгрузка проверены регрессией на работающем стенде.


## PHASE 5 — Пробы и цепочка ответственного хранения · 2026-09-24

### Добавлено
- **Проба как самостоятельная сущность** (`samples`): собственный номер `TR-SMP-2026-00001`, тип, метод отбора, культура из общего справочника, количество, тара, партия, место, инструкции. Принадлежит заявке всегда и инспекции — когда её взяли в поле; одна инспекция может иметь ноль, одну или много проб.
- **Жизненный цикл из 10 состояний**: черновик → отобрана → зарегистрирована → опломбирована → отправлена → получена лабораторией → принята, плюс отклонена, приостановлена и отменена. `SampleWorkflowService` — единственное место, которое пишет статус.
- **Цепочка ответственного хранения** (`sample_custody_events`) — центральная часть фазы. Отдельная таблица от статусов, потому что это разные вопросы: проба может трижды сменить руки, не меняя статуса, а «в пути» — это место, а не решение. 13 типов событий, от отбора до приёмки в лаборатории.
- **Неизменяемость цепочки, обеспеченная грантами, а не дисциплиной**: у роли `gsi_app` есть `SELECT` и `INSERT` на эту таблицу и нет `UPDATE`/`DELETE`; эндпоинтов `PATCH` и `DELETE` не существует. Ошибку исправляет событие `correction` со ссылкой на исправляемое — оригинал остаётся на таймлайне зачёркнутым, и то, что кто-то поменял свою версию событий, тоже сохраняется.
- **Статус и физическое событие пишутся в одной транзакции.** Проба со статусом «отправлена», у которой в цепочке нет отправки, была бы хуже отсутствия записи.
- **Пломбы**: номер, тип, кто и когда опломбировал, состояние, время и автор вскрытия. Нарушенная при приёмке пломба фиксируется автоматически.
- **Регистрация замораживает личность пробы**: после неё тип, метод, культуру, количество, партию, тару и данные об отборе изменить нельзя — иначе пломба, этикетка и бумаги лаборатории описывали бы что-то другое. Примечания остаются открытыми, пока проба у нас.
- **Отправка и приёмка**: лаборатория назначения, перевозчик, трек-номер, число мест; на приёмке — состояние пломбы и пробы. Граница с лабораторией — приём либо отклонение с обязательной причиной из словаря; отклонённая проба возвращается в офис.
- **Лаборатории** (`laboratories`) как минимальный справочник: свои и внешние, с экраном администрирования. Без него отправка была бы кнопкой, которой некуда отправлять: миграция намеренно не создаёт ни одной лаборатории, а справочник, заполняемый только через SQL, — это ровно та функция, которая «как будто работает». Закрытая лаборатория уходит из списка отправки, но остаётся в базе: пробы, уже отправленные туда, ссылаются на строку, которая по-прежнему говорит, чем она была. Анализов, методик и результатов здесь нет — это PHASE 6.
- **Наборы и производные пробы**: `sample_group` объединяет первичную, контрпробу и архивную одной партии, `parent_sample_id` — задел под деление на навески, и не более того.
- **Фото и документы** пробы в том же объектном хранилище: фотография пломбы, тары, накладной. Принимаются и PDF — накладная такое же доказательство, как снимок.
- **Печатная этикетка с QR.** QR ведёт в приложение, а не на публичную страницу: этикетка приклеена к пакету в порту, и культура, клиент и назначение груза — не дело того, кто её поднял.
- Экраны: список проб с быстрыми фильтрами «отобраны сегодня / ждут регистрации / ждут отправки / в пути / отклонены» и полным набором фильтров (статус, тип, лаборатория назначения, клиент, культура, пробоотборщик, период; страна и офис — через переключатель филиала), карточка пробы с цепочкой хранения, вкладка «Пробы» на карточках инспекции и заявки, мобильная форма отбора, прилипающая панель действий, экран «Лаборатории».
- 14 прав `sample.*`, выведенных из уже имеющихся, и отдельная граница для лабораторных ролей.
- Экспорт проб — двенадцатый раздел выгрузки; одиннадцать существующих не тронуты.
- 50 новых тестов: 14 на машину состояний и 36 на сквозной сценарий — от отбора на причале до приёмки лабораторией, включая неизменяемость цепочки, конкурентную нумерацию, области видимости и границу прав лаборатории.

### Изменено
- **Сид отчётов стал идемпотентным.** `SEED_REPORTS` означал «выпусти столько ещё», и при 956 утверждённых заявках каждый рестарт API молча добавлял сорок отчётов — за одну сессию разработки их стало 520 вместо 40. Теперь это «столько отчётов должно быть в реестре»: второй запуск не выпускает ничего. Зафиксировано отдельным тестом.
- Демо-данные: добавлены пробоотборщик и руководитель лаборатории в Стамбуле и три демо-лаборатории (две свои и одна независимая). Легаси-поле `users.role` отделено от кода роли RBAC — эти списки перестали совпадать ещё в PHASE 1.
- Тест изоляции «инспектор видит только свои заявки» проверял устаревшее правило: с PHASE 4 инспектор законно видит заявку и через назначение на её инспекцию. Проверка переписана под фактическое правило.

### Сохранено без изменений
**Из существующих данных не создано ни одной пробы.** В базе есть 162 заявки типа «отбор проб» с заполненными чек-листами, но поле `value` у всех пунктов пусто — ни номера пломбы, ни метода, ни количества там нет. Собрать из этого пробу значило бы её выдумать; история остаётся в чек-листе инспекции и задокументирована. Лаборатории миграция тоже не изобретает: ни одно поле в базе не говорит, в каких офисах они есть.

Заявки, инспекции, чек-листы, замечания, замеры, фотографии, отчёты, QR-верификация, финансы, активы, загрузка и выгрузка проверены регрессионными тестами после миграции.

## PHASE 4 — Инспекции · 2026-09-24

### Добавлено
- **Инспекция как отдельная сущность** (`inspections`): полевая работа по заявке со своим номером `TR-INS-2026-00001`, видом услуги из общего каталога, плановыми и фактическими датами, условиями на объекте, наблюдениями и заключением. Одна заявка может держать несколько инспекций — погрузка и выгрузка, повторная инспекция, разные услуги по одной номинации.
- **Жизненный цикл из 8 состояний**: черновик → запланирована → в работе → завершена → на проверке → утверждена, плюс приостановлена и отменена. Диаграмма и правила — [`WORKFLOWS.md`](WORKFLOWS.md).
- **Движок переходов** (`InspectionWorkflowService`) — той же формы, что у заявки: единственное место, которое пишет статус, и одинаковый путь для каждого перехода. Фактическое начало и окончание ставит он, а не форма.
- **Защита от тихого изменения**: утверждённую инспекцию нельзя править вовсе — только `reopen` с письменной причиной, которая попадает и в историю, и в аудит. Запрет действует и со стороны заявки: пункт чек-листа подчиняется блокировке своей инспекции, даже если открыть его через старый эндпоинт заявки.
- **История статусов** (`inspection_status_history`) — только на добавление, с причиной и автором.
- **Команда на инспекции** (`inspection_assignments`), независимая от команды на заявке: две инспекции по одной номинации могут выполнять разные люди в разные дни.
- **Замечания** (`inspection_findings`): важность `info | minor | major | critical`, статус `open | acknowledged | resolved | withdrawn`, рекомендация и признак «только внутреннее». Статус замечания меняется и после утверждения — это работа по следам инспекции, а не переписывание её текста.
- **Замеры** (`inspection_measurements`): одна расширяемая таблица вместо колонки на каждую величину — тип, значение, единица, место замера.
- **Фотографии с категориями** (`general`, `cargo`, `damage`, `seal`, `container`, `document`, `equipment`, `sampling`, `other`) и подписями, по-прежнему с GPS, временем съёмки и sha256.
- **Полевой экран для телефона**: секции чек-листа, крупные кнопки результата, камера через `<input capture>`, прилипающая снизу панель действий и **пакетное автосохранение** — правки копятся и уходят одним запросом через 800 мс, при уходе со страницы и при сворачивании приложения. Состояние показывается честно: «Сохранение…», «Сохранено», «Не сохранено» с повтором; неудача не теряет правки.
- **Обязательные пункты чек-листа** (`is_required`, по умолчанию `true`): инспекцию нельзя завершить, пока они без ответа, и счётчик оставшихся виден и в списке, и на карточке.
- **Секции чек-листа** (`documents`, `vessel`, `cargo`, `operations`, `observations`) — чтобы на телефоне работать блоком, а не листать плоский список из тридцати вопросов под дождём.
- Экраны: «Инспекции» / «Мои инспекции» с постраничным списком, поиском, фильтрами и карточками вместо таблицы на узком экране; карточка инспекции вкладками (детали, чек-лист, замечания, замеры, фото, исполнители, история); вкладка «Инспекции» на карточке заявки с созданием новой.
- 13 прав `inspection.*`, выданных не вручную, а выведенных из уже имеющихся прав на заявки; пробоотборщик получил полевой набор отдельно.
- 46 новых тестов: 13 на машину состояний и 33 на полный сценарий — от открытия заявки до утверждения, повторного открытия и проверки, что утверждённую инспекцию не переписать; плюс регрессии на чек-лист заявки, отчёты, QR, финансы, активы и выгрузку. Загрузка фотографии наконец покрыта тестом — включая превью, контрольную сумму и отказ для файла, который не изображение: раньше этот путь не проверялся ни одним тестом.

### Изменено
- **Чек-лист и фотографии переехали к инспекции** — без копирования и без потерь: `job_checklist_items` и `media_attachments` получили `inspection_id` и сохранили `job_id`, поэтому отчёты, политики RLS и все существовавшие запросы работают без правок. Миграция 013 создала по одной инспекции на каждую из 956 заявок и привязала к ним все 5701 пункт чек-листа.
- Ключ пункта чек-листа был уникален в пределах заявки — стал уникален в пределах инспекции: две инспекции по одной заявке законно задают одни и те же вопросы дважды.
- **Создание заявки открывает её первую инспекцию** — ту, что несёт чек-лист. Вкладка «Полевые работы» на карточке заявки стала вкладкой «Инспекции».
- `app_is_assigned(job)` расширена: кто работает на инспекции, тот видит и её заявку.
- Версия шаблона чек-листа поднята до `v2-draft` (добавились секции и обязательность); строки, снятые раньше, так и остаются `v1-draft` — в этом и смысл того, что версия стоит на строке.
- `ChecklistService` больше не пишет статус заявки напрямую: первый ответ в поле начинает заявку через `JobWorkflowService`, с записью в истории.
- Демо-данные: каждая seed-заявка получает инспекцию с чек-листом, чтобы свежая установка выглядела так же, как мигрированная база.

### Сохранено без изменений
Эндпоинты чек-листа заявки (`/jobs/:id/checklist`) остались на месте и продолжают работать. Отчёты, QR-верификация, счета, расходы, активы, выгрузка и загрузка проверены регрессионными тестами после миграции.

## PHASE 3 — Заявки · 2026-09-24

### Добавлено
- **Жизненный цикл заявки** из 14 состояний: черновик → подтверждена → назначена → в работе → отбор проб → лаборатория → подготовка отчёта → на проверке → утверждена → завершена → выставлен счёт → закрыта, плюс приостановлена и отменена. Диаграмма и правила — [`WORKFLOWS.md`](WORKFLOWS.md).
- **Движок переходов** (`JobWorkflowService`): единственное место в системе, которое пишет статус. Каждый переход проходит одинаковый путь — право, допустимость перехода, обязательная причина, бизнес-правила, запись, история, аудит, событие — внутри одной транзакции.
- **Бизнес-правила с внятными отказами**: заявку нельзя подтвердить без клиента, офиса, услуги и запрошенной даты; сдать без заполненного чек-листа; завершить без выпущенного отчёта; закрыть с неоплаченным счётом. Сообщение объясняет, чего не хватает, а не просто «422».
- **История статусов** (`job_status_history`) — только на добавление, с причиной и автором. Существующим 956 заявкам история восстановлена.
- **Команда на заявке** (`job_assignments`): ведущий инспектор, инспектор, пробоотборщик, координатор лаборатории, проверяющий отчёт, координатор операций. Ведущий один — это гарантирует индекс. Снятие не удаляет строку.
- **Новые поля заявки**: приоритет, контактное лицо клиента, контракт, номер у клиента, запрашивающий офис, город, объект инспекции (судно/склад/терминал/контейнер), номер контейнера, транспортная ссылка, внутренние заметки, запрошенная дата.
- **Оптимистичная блокировка**: форма присылает версию, с которой открыта; если в базе новее — 409 с понятным сообщением вместо затёртой чужой правки.
- **Архив и восстановление** заявок, отдельно от бизнес-статуса «отменена».
- **Просрочка** как вычисляемый признак (плановая дата в прошлом при активном статусе), а не как ещё один статус.
- Шов для уведомлений (`JobEventsService`): `job.status_changed`, `job.assigned`, `job.unassigned` — PHASE 10 подключит к нему центр уведомлений.
- Экраны: постраничный список с поиском по семи полям, фильтрами, сортировкой по шести колонкам и переключателем «Мои заявки»; карточка вкладками (детали, исполнители, полевые работы, отчёты, счета, история) с хронологией статусов и кнопками допустимых действий вместо выпадающего списка статусов; форма создания по разделам с сохранением черновика.
- 32 новых теста: полный сценарий от черновика до закрытия, десять одновременных созданий заявок, области видимости, архив, регрессии на отчёты, QR, финансы, чек-лист и выгрузку.

### Изменено
- **Статус `new` переименован в `confirmed`** (миграция 012). Переименование значения enum, а не перенос строк: 956 существующих заявок перешли в новый словарь без единого UPDATE. В прежней модели «новая» означала полностью оформленную заявку, ожидающую инспектора, — это и есть «подтверждена».
- `under_review` осталось как есть: это и есть стадия REVIEW, а переименование стоило бы данных и переводов без выигрыша.
- **Список заявок стал постраничным** (`{ rows, total, limit, offset }`) — ломающее изменение `/api/jobs`, все вызовы обновлены.
- Право `job.delete` переименовано в `job.archive`: удаления заявок в системе никогда и не было.
- Назначение исполнителя теперь отдельный ресурс (`/jobs/:id/assignments`) вместо поля в заявке; прежние эндпоинты жизненного цикла (`/start`, `/submit`, `/approve`, `/cancel`, `/return`) сохранены и вызывают новый движок.
- Демо-данные: заявки получили приоритеты, реалистичный хвост незавершённых статусов, команды исполнителей и историю.

### Сохранено без изменений
Таблица осталась `inspection_jobs`, номер — `TR-J-2026-00001` из счётчика в транзакции: и то и другое уже работает, уникально и напечатано на выпущенных документах. Отчёты, QR-верификация, счета, расходы, чек-листы, активы, выгрузка и загрузка проверены регрессионными тестами после миграции.

## PHASE 2 — CRM · 2026-09-24

### Добавлено
- **Контактные лица клиента** (`client_contacts`): у инспекционной компании переписка идёт с операционным отделом, с документацией и с бухгалтерией — одного поля «контакт» не хватало. Один контакт помечается основным (гарантируется уникальным индексом).
- **Контракты** (`contracts`): номер, предмет, статус, срок действия, валюта и сумма, отсрочка платежа, Инкотермс, покрываемые услуги, плюс **подписанный документ** в объектном хранилище (PDF, скан или Word; ссылка выдаётся короткоживущая).
- **Карточка клиента вкладками**: карточка, контакты, контракты, заявки, отчёты, счета, история. Вкладка открывается по ссылке (`?tab=contracts`).
- **История клиента** — журнал аудита, отфильтрованный по клиенту и всему, что с ним связано: контактам, контрактам, заявкам, счетам и отчётам.
- **Экран контрактов по группе** с фильтром «истекают в течение 30 / 60 / 90 дней»: контракт, истёкший посреди отгрузки, должен быть виден заранее.
- Права `contract.read` и `contract.manage`. Коммерческие условия видят офисные и коммерческие роли; полевым и лабораторным они не нужны и не выдаются.
- Заявка может ссылаться на контракт (`inspection_jobs.contract_id`); свободный текстовый номер контракта остался — не у каждой заявки есть контракт в системе.
- 14 тестов CRM: страницы списка без повторов, перенос признака «основной», проверка срока действия, дубль номера контракта, загрузка и скачивание документа, история клиента, закрытость от полевых ролей.

### Изменено
- **Список контрагентов теперь постраничный** (`{ rows, total, limit, offset }`), с общим счётчиком и переключателем страниц; в таблице появились основной контакт и число действующих контрактов. Прежний ответ-массив заменён — это ломающее изменение API, все внутренние вызовы обновлены.
- Создание и изменение контрагента пишутся в журнал аудита (раньше писалось только архивирование).
- Даты без времени (сроки контракта) больше не сдвигаются на сутки в офисах с другим часовым поясом.

## PHASE 1 — Организация, права, аудит · 2026-09-23

### Добавлено
- **Иерархия группы**: `organizations → countries → branches (офисы) → departments → users`. Офис остался таблицей `branches` — данные не переносились. Страна проставляется по ISO-коду офиса; офис в новой стране создаёт страну автоматически.
- **RBAC**: 45 прав и 15 ролей с областями видимости (группа / страна / офис / свои записи). Роли — данные: состав прав меняется на экране, без релиза.
- **Журнал аудита** (`audit_logs`), только на добавление — у приложения нет прав `UPDATE`/`DELETE` на эту таблицу. Пишутся входы (включая неудачные, с причиной), создание и изменение записей, все переходы статусов заявок, выпуск отчётов, изменения ролей и прав, архивирование.
- Экраны **«Роли и права»** и **«Журнал действий»** (en / ru / tr), API организации: страны и отделы.
- Тесты: изоляция по офису и по стране, мгновенный отзыв роли, запись в аудит, неизменяемость журнала, архивирование (всего 79 тестов).

### Изменено
- Доступ к действиям проверяется по праву (`@RequirePermission('job.approve')`), а не по имени роли — 39 маршрутов. Списки ролей, которые раньше дублировались в SQL, в API и во фронтенде, исчезли.
- Политики RLS переписаны в терминах области видимости: `app_can_see_branch()` вместо сравнения с одним филиалом. Инспектор по-прежнему видит только свои заявки — теперь это следствие области `own`, а не имени роли.
- Роли пользователя читаются на каждом запросе (кэш 10 секунд), поэтому отзыв роли действует почти сразу, а не после истечения токена.
- Интерфейс строится по правам: меню, кнопки и маршруты показывают только то, что человеку доступно.
- **Удаление заменено на архивирование** для контрагентов, заявок, счетов, расходов и активов. Архив скрыт самими политиками, а не фильтром в каждом запросе.

### Известные ограничения на конец фазы
- Экранов управления странами и отделами пока нет — только API.
- Восстановление из архива доступно на уровне базы (режим `includeArchived`), экрана ещё нет.
- Refresh-токены по-прежнему без серверного отзыва (перенесено в PHASE 12).

## PHASE 0 — Аудит и стабилизация · 2026-09-23

### Добавлено
- `docs/CURRENT_STATE.md` — аудит существующего кода: стек, модули, схема БД, API, найденные проблемы (критические, важные, техдолг), соответствие целевым требованиям.
- `docs/IMPLEMENTATION_PLAN.md` — фазы 0–13 с задачами и статусами, приёмочный сценарий из 24 шагов.
- **Инфраструктура тестов** (её не было вообще): vitest для модульных тестов, vitest + supertest для интеграционных, отдельная одноразовая база `gsi_test`, сервис `test` в docker-compose с профилем `test`.
  - модульные: разбор CSV/XLSX и приведение типов при импорте, запись CSV при выгрузке, машина статусов заявки;
  - интеграционные: аутентификация и обращение с токенами, изоляция филиалов и проверка ролей (RLS), полный жизненный цикл заявки от создания клиента до выпуска отчёта и публичной верификации.
- ESLint для backend.
- Заголовки безопасности (`helmet`) и ограничение частоты запросов: общий бюджет на API и отдельный, жёсткий, на вход в систему.
- Идентификатор запроса (`X-Request-Id`), сквозной в логах и в теле ошибки.
- Структурированное логирование запросов (JSON в production, читаемый вид в разработке).
- Health-проверки: `/api/health/live`, `/api/health/ready`, `/api/health` с проверкой БД и объектного хранилища.

### Изменено
- Единый обработчик ошибок: пользователь больше не видит `500 Internal Server Error` со служебными деталями — только понятное сообщение и ссылочный идентификатор, по которому ошибку можно найти в логе. Обработка ошибок PostgreSQL сохранена и встроена в общий обработчик.
- Демо-данные отделены от эталонных: тесты и разработка могут поднимать базу без годовой истории счетов (`SEED_DEMO_VOLUME=false`), курсы валют засеваются всегда.
- Засев демо-данных в production логирует явное предупреждение.

### Известные ограничения на конец фазы
- Иерархии «организация → страна → офис» и granular permissions ещё нет — это PHASE 1.
- Журнала аудита ещё нет — PHASE 1.
- Пагинации в списках ещё нет — PHASE 3 и PHASE 12.

---

## До аудита (MVP-1 … MVP-3, ветка `feat/mvp-1`)

Хронология по коммитам: базовая платформа (филиалы, RLS, аутентификация, CRM, заявки, чек-лист, фото, PDF-отчёты с QR), финансовый блок (счета, расходы, двойная запись, мультивалютная консолидация, живой дашборд), сравнение филиалов и карточка филиала, справочники культур и портов с фильтрами, турецкий язык, активы и амортизация, выгрузка данных, загрузка данных из Excel/CSV.
