# IMPLEMENTATION_PLAN — план доведения GSI ONE до production

Основан на аудите [`CURRENT_STATE.md`](CURRENT_STATE.md). Статусы: **DONE** / **IN PROGRESS** / **TODO** / **BLOCKED**.

Правило работы: каждая фаза — полный вертикальный срез (миграция → сервис → API → права → аудит → UI → тесты → документация). Фаза не считается завершённой, пока не проходят сборка и тесты.

---

## PHASE 0 — Аудит и стабилизация · DONE (2026-09-23)

| # | Задача | Статус |
|---|---|---|
| 0.1 | Полный аудит репозитория, `docs/CURRENT_STATE.md` | DONE |
| 0.2 | План работ, `docs/IMPLEMENTATION_PLAN.md` | DONE |
| 0.3 | Инфраструктура тестов внутри Docker (vitest + supertest), первые тесты | DONE — 68 тестов (36 модульных, 32 интеграционных) |
| 0.4 | ESLint + typecheck одной командой | DONE |
| 0.5 | Заголовки безопасности (helmet) и ограничение частоты запросов на `/auth/*` | DONE — 10 попыток входа в минуту |
| 0.6 | Единый формат ошибок API + скрытие внутренних деталей от пользователя | DONE — с `requestId` в ответе и логе |
| 0.7 | Структурированное логирование, расширенный `/api/health` | DONE — `live` / `ready` / сводка |
| 0.8 | Отделение демо-данных от production (`SEED_DEMO` запрещён при `NODE_ENV=production`) | DONE — плюс `SEED_DEMO_VOLUME` для лёгкой базы |

Команда проверки: `docker compose --profile test run --rm test` (unit + integration), `npm run typecheck`, `npm run lint` в том же сервисе.

## PHASE 1 — Фундамент: организация, RBAC, аудит · TODO

| # | Задача |
|---|---|
| 1.1 | Миграция: `organizations`, `countries`, `offices`, `departments`; перенос `branches` без потери данных |
| 1.2 | Расширение контекста доступа: `app.office_id`, `app.country_id`, `app.scope`; обновление всех RLS-политик |
| 1.3 | Таблицы `permissions`, `roles`, `role_permissions`, `user_roles` + сидинг прав |
| 1.4 | `PermissionsGuard` и декоратор `@RequirePermission('job.approve')`; замена разбросанных списков ролей |
| 1.5 | `audit_logs` + `AuditService`; логирование логина, изменений, переходов статусов, утверждений, выгрузок |
| 1.6 | Мягкое удаление (`deleted_at`) на бизнес-таблицах; `DELETE` → архивирование |
| 1.7 | Экран администрирования: страны, офисы, отделы, роли, права, журнал аудита |
| 1.8 | Тесты: изоляция по офису и стране, отказ по праву, запись в аудит |

## PHASE 2 — CRM · TODO

| # | Задача |
|---|---|
| 2.1 | `client_contacts`, `contracts`, документы клиента, заметки |
| 2.2 | Карточка клиента: Overview / Contacts / Jobs / Contracts / Documents / Invoices / Activity |
| 2.3 | Архивирование вместо удаления, поиск и фильтры, пагинация |

## PHASE 3 — Заявки (Jobs) · TODO

| # | Задача |
|---|---|
| 3.1 | Расширение модели: приоритет, запрашивающий и исполняющий офис, объект (судно/склад/терминал), вложения |
| 3.2 | `job_assignments` (несколько исполнителей), `job_status_history` |
| 3.3 | Расширенная машина статусов: DRAFT → CONFIRMED → ASSIGNED → IN PROGRESS → SAMPLING → LAB → REPORT → REVIEW → APPROVED → COMPLETED → INVOICED → CLOSED, плюс ON HOLD / CANCELLED |
| 3.4 | Единый workflow-движок (validate → authorize → execute → audit) |
| 3.5 | Пагинация и серверная фильтрация списка заявок |

## PHASE 4 — Инспекции · TODO

| # | Задача |
|---|---|
| 4.1 | `inspections` как отдельная сущность: тип, плановое и фактическое время, место, статус |
| 4.2 | `inspection_findings`, измерения, подпись, GPS |
| 4.3 | Мобильный экран инспектора: мои задания → старт → чек-лист → фото → образец → завершение |
| 4.4 | Автосохранение черновика инспекции |

## PHASE 5 — Образцы · TODO

| # | Задача |
|---|---|
| 5.1 | `samples` с собственным номером (`GSI-SMP-2026-000001`), пломба, контейнер, состояние |
| 5.2 | `sample_status_history` и цепочка ответственного хранения (chain of custody) |
| 5.3 | Workflow: COLLECTED → REGISTERED → SENT TO LAB → RECEIVED → TESTING → COMPLETED → ARCHIVED |

## PHASE 6 — Лаборатория / LIMS · TODO

| # | Задача |
|---|---|
| 6.1 | `laboratories`, `test_methods`, `lab_tests`, `test_requests`, `test_results`, приборы, аналитики |
| 6.2 | Ввод результатов с границами (min/max/target), сверка с MRL |
| 6.3 | Workflow: REQUESTED → RECEIVED → ASSIGNED → IN TESTING → RESULT ENTERED → REVIEW → APPROVED |
| 6.4 | Изменение утверждённого результата — только по праву и с записью в аудит |
| 6.5 | Связь культур (`commodities.lab_methods`, уже засеяны) с методиками |

## PHASE 7 — Отчёты и сертификаты · TODO

| # | Задача |
|---|---|
| 7.1 | `report_versions`: выпущенная версия неизменяема, правка создаёт новую ревизию |
| 7.2 | `certificates` с собственной нумерацией и верификацией по QR |
| 7.3 | Workflow: DRAFT → REVIEW → CHANGES REQUESTED → APPROVED → ISSUED |
| 7.4 | Включение результатов лаборатории в отчёт |
| 7.5 | Фундамент для цифровой подписи |

## PHASE 8 — Финансы · TODO

| # | Задача |
|---|---|
| 8.1 | `services`, `prices`, `quotes` |
| 8.2 | `payments` как сущность; кредиторская задолженность по расходам |
| 8.3 | Себестоимость заявки и маржа: Revenue / Costs / Margin в карточке Job |
| 8.4 | Печатная форма счёта (есть), напоминания о просрочке |

## PHASE 9 — Аналитика · TODO

| # | Задача |
|---|---|
| 9.1 | Операционная аналитика: заявки по месяцам, клиентам, странам, офисам, услугам |
| 9.2 | Сроки прохождения: инспекция, лаборатория, отчёт (turnaround time) |
| 9.3 | Загрузка сотрудников и лабораторий |
| 9.4 | Executive dashboard по ролям |

## PHASE 10 — Документы и уведомления · TODO

| # | Задача |
|---|---|
| 10.1 | Общий реестр `documents`: тип, категория, версия, связанная сущность, метаданные |
| 10.2 | Поддержка PDF / Word / Excel / изображений с валидацией |
| 10.3 | `notifications` + центр уведомлений в интерфейсе |
| 10.4 | Адаптер e-mail (без жёсткой привязки к провайдеру) |
| 10.5 | Глобальный поиск по номерам заявок, образцов, отчётов, сертификатов, счетов и клиентам |

## PHASE 11 — PWA, мобильный интерфейс, i18n · TODO

| # | Задача |
|---|---|
| 11.1 | manifest, иконки, service worker, установка на телефон |
| 11.2 | Нижняя навигация и touch-friendly элементы для инспектора и лаборанта |
| 11.3 | Фундамент офлайна: очередь операций и локальный кэш заданий |
| 11.4 | Расширение языков без правки UI |

## PHASE 12 — Безопасность, тесты, производительность · TODO

| # | Задача |
|---|---|
| 12.1 | Отзыв refresh-токенов, выход со всех устройств |
| 12.2 | Перенос refresh-токена в httpOnly cookie |
| 12.3 | Индексы под реальные запросы, устранение N+1, серверная фильтрация везде |
| 12.4 | Интеграционные тесты приёмочного сценария целиком |
| 12.5 | Нагрузочная проверка списков на объёме демо-данных |

## PHASE 13 — Готовность к production · TODO

| # | Задача |
|---|---|
| 13.1 | `docs/DEPLOYMENT.md`, production-конфигурация Docker, HTTPS, домены |
| 13.2 | Резервное копирование БД и объектного хранилища |
| 13.3 | Логи, метрики, health-проверки в оркестраторе |
| 13.4 | `THIRD_PARTY_LICENSES.md`, проверка совместимости лицензий |
| 13.5 | Финальный прогон приёмочного сценария из ТЗ (24 шага) |

---

## Приёмочный сценарий

Считается выполненным только целиком, от начала до конца, на чистой базе:

1. Admin создаёт страну → 2. офис → 3. пользователя → 4. назначает роль → 5. Sales создаёт клиента → 6. Operations создаёт заявку → 7. Office Manager назначает инспектора → 8. Inspector видит заявку на телефоне → 9. начинает инспекцию → 10. загружает фото → 11. заполняет чек-лист → 12. создаёт образец → 13. завершает инспекцию → 14. лаборатория принимает образец → 15. аналитик вводит результаты → 16. Lab Manager утверждает → 17. Report Reviewer готовит отчёт → 18. Manager утверждает → 19. система выпускает финальную версию → 20. Finance выставляет счёт → 21. регистрируется оплата → 22. заявка закрывается → 23. результат виден в аналитике → 24. все критичные действия видны в журнале аудита.

Текущее состояние сценария: шаги 5, 6, 7, 9–11, 13, 20, 21 работают; остальные требуют реализации.

---

## Журнал фаз

| Дата | Фаза | Что сделано |
|---|---|---|
| 2026-09-23 | PHASE 0 | Аудит завершён, созданы `CURRENT_STATE.md` и `IMPLEMENTATION_PLAN.md`; появилась инфраструктура тестов (68 тестов), линтер, заголовки безопасности, ограничение частоты запросов, единый формат ошибок, health-проверки, отделение демо-данных |
