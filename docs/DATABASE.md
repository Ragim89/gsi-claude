# База данных GSI ONE

PostgreSQL 16. Схема развивается только миграциями (`apps/api/migrations/*.sql`), которые применяются раннером `src/db/migrate.ts` — по одной в транзакции, с записью в `schema_migrations`. Вручную схему не меняют.

Актуально после миграции 014.

---

## Принципы

1. **Изоляция — в базе, а не в коде.** Приложение подключается ролью `gsi_app` (`NOSUPERUSER`, `NOBYPASSRLS`). На всех бизнес-таблицах включён Row-Level Security; политики читают контекст транзакции (`app.user_id`, `app.branch_id`, `app.country_id`, `app.scope`, `app.permissions`). Подробнее — [`ROLES_AND_PERMISSIONS.md`](ROLES_AND_PERMISSIONS.md).
2. **Дочерняя запись не может оказаться в чужом офисе.** Составные внешние ключи `(id, branch_id)` плюс триггеры наследования (`trg_inherit_job_branch`, `trg_inherit_client_branch`, `trg_inherit_invoice_branch`, `trg_inherit_asset_branch`).
3. **Бизнес-данные не удаляются.** `deleted_at` + `app_is_live()` в политиках: архив скрыт от всех запросов, а не от тех, где не забыли фильтр.
4. **Деньги — `numeric`**, никогда не float. Каждая сумма хранится в валюте офиса и в валюте консолидации по курсу даты.
5. **Нумерация документов — счётчик в транзакции** (`next_doc_number`), не `MAX(id)+1`.

---

## Таблицы

### Организация и доступ

| Таблица | Назначение |
|---|---|
| `organizations` | группа (одна запись: GSI) |
| `countries` | страна присутствия; создаётся автоматически при открытии офиса в новой стране |
| `branches` | **офис** = юридическое лицо: валюта учёта, бланк, реквизиты, глава |
| `departments` | отделы внутри офиса |
| `users` | сотрудники; `branch_id` — офис, `department_id` — отдел |
| `permissions`, `roles`, `role_permissions`, `user_roles` | RBAC: 51 право, 15 ролей с областями видимости |
| `audit_logs` | журнал действий, только на добавление |

### Операции

| Таблица | Назначение |
|---|---|
| `inspection_jobs` | заявка на инспекцию — центральная операционная сущность |
| `job_status_history` | каждое изменение статуса; только на добавление |
| `job_assignments` | кто работает на заявке и в какой роли |
| `inspections` | инспекция — полевая работа по заявке; одна заявка может держать несколько |
| `inspection_status_history` | каждое изменение статуса инспекции; только на добавление |
| `inspection_assignments` | кто работает на инспекции и в какой роли |
| `inspection_findings` | замечания: важность, статус, рекомендация, признак «только внутреннее» |
| `inspection_measurements` | замеры на объекте: тип, значение, единица, место |
| `job_checklist_items` | полевой чек-лист (снимок шаблона); с миграции 013 принадлежит инспекции |
| `media_attachments` | фото с GPS, временем съёмки и sha256; с миграции 013 — с инспекцией, категорией и подписью |
| `samples` | проба: что отобрали, чем опломбировали, куда отправили |
| `sample_status_history` | каждое изменение статуса пробы; только на добавление |
| `sample_custody_events` | цепочка ответственного хранения; **только на добавление, без UPDATE и DELETE** |
| `laboratories` | куда отправляют пробы: свои лаборатории и внешние |
| `reports` | выпущенные отчёты с токеном верификации |
| `doc_counters` | счётчики номеров по офису, типу документа и году |

### CRM

| Таблица | Назначение |
|---|---|
| `clients` | контрагенты |
| `client_contacts` | контактные лица; ровно один основной на клиента |
| `contracts` | контракты с условиями и подписанным документом |

### Финансы и активы

| Таблица | Назначение |
|---|---|
| `invoices`, `invoice_lines` | счета клиентам |
| `expenses` | расходы офиса |
| `ledger_entries` | двойная запись — источник правды для дашборда |
| `finance_daily_agg` | инкрементальный агрегат, обновляется триггером + `pg_notify` |
| `fx_rates` | курсы валют к валюте консолидации |
| `assets`, `asset_depreciation` | основные средства и амортизация |

### Справочники

`commodities` (культуры с методами лаборатории), `ports`.

---

## Заявка: ключевые поля

| Поле | Смысл |
|---|---|
| `job_number` | `TR-J-2026-00001` — офис, тип документа, год, порядковый номер |
| `branch_id` | офис, отвечающий за работу |
| `requesting_branch_id` | офис, который попросил, если это другой офис |
| `client_id`, `client_contact_id`, `contract_id` | с кем и на основании чего |
| `client_reference` | номер этой заявки у клиента |
| `type` | тип услуги |
| `status`, `status_before_hold` | состояние и куда вернуться после снятия с паузы |
| `priority` | low / normal / high / urgent |
| `assigned_inspector_id` | ведущий (дублирует запись в `job_assignments`) |
| `requested_date` | дата, о которой просил клиент (календарный день) |
| `scheduled_at` | когда мы планируем быть на месте (момент времени) |
| `location`, `city`, `port_id` | где |
| `object_kind`, `vessel_or_object`, `container_no`, `transport_ref` | на чём |
| `commodity_id`, `commodity`, `quantity_value`, `quantity_unit`, `quantity` | что и сколько |
| `instructions` | что просил клиент |
| `internal_notes` | внутреннее, клиенту не печатается |
| `version` | оптимистичная блокировка; растёт триггером |
| `deleted_at` | архив (не то же самое, что статус `cancelled`) |

Почему таблица называется `inspection_jobs`, а не `jobs`: переименование затронуло бы шесть миграций, пять внешних ключей, все политики RLS, выпущенные отчёты и проводки — без единого выигрыша.

---

## Инспекция: ключевые поля

| Поле | Смысл |
|---|---|
| `inspection_number` | `TR-INS-2026-00001` — тот же формат, что у остальных документов |
| `job_id`, `branch_id` | заявка и офис; составной ключ `(job_id, branch_id)` не даёт инспекции уехать в чужой офис |
| `type` | вид услуги — из того же каталога `service_type`, что и у заявки, а не второй параллельный список |
| `status`, `status_before_hold` | состояние и куда вернуться после снятия с паузы |
| `lead_inspector_id` | ведущий (дублирует запись в `inspection_assignments`) |
| `scheduled_start`, `scheduled_end` | когда планировали |
| `actual_start`, `actual_end` | когда начали и закончили — ставит движок, а не форма |
| `weather_conditions`, `site_conditions` | условия на объекте |
| `general_observations`, `conclusion` | что инспектор увидел в целом и к чему пришёл |
| `internal_notes` | внутреннее, клиенту не печатается |
| `reviewed_by`, `reviewed_at`, `review_comment` | кто утвердил либо вернул и с какой формулировкой |
| `version` | оптимистичная блокировка; растёт триггером |
| `deleted_at` | архив (не то же самое, что статус `cancelled`) |

### Что произошло с чек-листом и фотографиями

Ничего не копировалось и не удалялось. `job_checklist_items` и `media_attachments` получили `inspection_id` и сохранили `job_id` — поэтому отчёты, политики RLS и все существовавшие запросы продолжают работать без правок.

Миграция 013 создала по одной инспекции на каждую существующую заявку (статус выведен из статуса заявки), привязала к ней все 5701 пункт чек-листа и все фотографии, восстановила историю одной честной записью и перенесла назначенного инспектора. Ключ элемента чек-листа был уникален в пределах заявки — теперь он уникален в пределах инспекции (частичный индекс), потому что две инспекции по одной заявке законно задают одни и те же вопросы дважды.

Дополнительно у пункта появились `section` (группировка на полевом экране) и `is_required` со значением по умолчанию `true` — ровно то, чего процесс заявки требовал и раньше.

### «Свои» записи

`app_is_on_inspection(inspection)` — `SECURITY DEFINER`: назначен на инспекцию, назначен на её заявку либо является ведущим. Функция `app_is_assigned(job)` расширена симметрично: кто работает на инспекции, тот видит и её заявку — иначе он не увидел бы и саму инспекцию, поскольку её запрос джойнит заявку ради номера и клиента.

---

## Проба: ключевые поля

| Поле | Смысл |
|---|---|
| `sample_number` | `TR-SMP-2026-00001` — тот же счётчик, что у всех документов |
| `job_id`, `inspection_id` | всегда заявка; инспекция — если пробу взяли в поле |
| `client_id`, `branch_id` | чей груз и какой офис отвечает |
| `sample_type`, `sampling_method` | что за проба и как отобрана |
| `commodity_id`, `commodity`, `quantity`, `unit` | что и сколько |
| `batch_lot_number`, `container_reference`, `container_type` | как найти партию и тару |
| `seal_number`, `seal_type`, `sealed_by`, `sealed_at`, `seal_state` | пломба и её состояние |
| `seal_broken_at`, `seal_broken_by` | когда и кем вскрыта |
| `destination_laboratory_id`, `courier`, `tracking_reference`, `package_count` | отправка |
| `received_condition`, `received_seal_condition` | что увидели на приёмке |
| `rejection_reason`, `rejection_notes` | почему лаборатория отказала |
| `sample_group`, `parent_sample_id` | набор одной партии и задел под навески PHASE 6 |
| `version` | оптимистичная блокировка; растёт триггером |
| `deleted_at` | архив (не то же самое, что статус `cancelled`) |

### Почему две таблицы, а не одна

`sample_status_history` отвечает на вопрос «что бизнес думал о пробе и когда», `sample_custody_events` — «где она была и у кого». Это разные вопросы: проба может сменить трёх держателей, не меняя статуса. Слить их в одну таблицу значило бы навсегда потерять возможность ответить на любой из них по отдельности.

Обе таблицы — только на добавление, и это обеспечено **грантами**, а не дисциплиной в коде: у `gsi_app` есть `SELECT` и `INSERT` и нет `UPDATE`/`DELETE`. Ошибочная запись исправляется событием `correction` со ссылкой `corrects_event_id`; оригинал остаётся.

### «Свои» записи

`app_sample_is_mine(sampled_by, created_by, job_id, inspection_id)` принимает **значения колонок**, а не идентификатор пробы. Политика, которая ищет строку в собственной таблице, не видит строку, вставляемую тем же оператором — подзапрос работает со снимком до команды, — и `INSERT … RETURNING` был бы отклонён именно для тех, кто отбирает пробы. Передача колонок снимает это полностью и заодно экономит один запрос.

Проба, отправленная в лабораторию, становится видна офису этой лаборатории (`app_sees_laboratory`): иначе принять её было бы некому.

---

## Нумерация документов

```sql
next_doc_number(branch_id, kind) → 'TR-J-2026-00001'
```

Счётчик — строка в `doc_counters (branch_id, kind, year)`, увеличиваемая `INSERT … ON CONFLICT DO UPDATE … RETURNING` внутри транзакции заявки. Два параллельных создания получают два разных номера: проверено тестом, который открывает десять заявок одновременно. Функция `SECURITY DEFINER` и проверяет, что вызывающий имеет отношение к офису.

Типы: `J` — заявка, `INS` — инспекция, `SMP` — проба, `R` — отчёт, `I` — счёт.

---

## Индексы

Частичные индексы «только живые записи» под то, как экраны действительно фильтруют:

```
jobs_active_idx      (branch_id, status)    WHERE deleted_at IS NULL
jobs_priority_idx    (branch_id, priority)  WHERE deleted_at IS NULL
jobs_scheduled_idx   (scheduled_at)         WHERE deleted_at IS NULL
jobs_requested_idx   (requested_date)       WHERE deleted_at IS NULL
jobs_updated_idx     (updated_at DESC)      WHERE deleted_at IS NULL
jobs_contract_ref_idx(contract_id)          WHERE contract_id IS NOT NULL
job_assignments_job_idx / _user_idx         WHERE removed_at IS NULL
job_status_history_job_idx (job_id, created_at)

inspections_job_idx       (job_id)              WHERE deleted_at IS NULL
inspections_status_idx    (branch_id, status)   WHERE deleted_at IS NULL
inspections_scheduled_idx (scheduled_start)     WHERE deleted_at IS NULL
inspections_lead_idx      (lead_inspector_id)   WHERE deleted_at IS NULL
inspections_updated_idx   (updated_at DESC)     WHERE deleted_at IS NULL
inspection_assignments_idx / _user_idx          WHERE removed_at IS NULL
inspection_history_idx    (inspection_id, created_at)
checklist_inspection_idx  (inspection_id)
media_inspection_idx      (inspection_id)

samples_job_idx / _inspection_idx / _client_idx        WHERE deleted_at IS NULL
samples_status_idx        (branch_id, status)          WHERE deleted_at IS NULL
samples_sampled_idx       (sampled_at DESC)            WHERE deleted_at IS NULL
samples_sampler_idx       (sampled_by)                 WHERE deleted_at IS NULL
samples_lab_idx           (destination_laboratory_id)  WHERE deleted_at IS NULL
samples_seal_idx          (lower(seal_number))         WHERE seal_number IS NOT NULL
samples_batch_idx         (lower(batch_lot_number))    WHERE batch_lot_number IS NOT NULL
sample_custody_idx        (sample_id, occurred_at)
sample_history_idx        (sample_id, created_at)
media_sample_idx          (sample_id)
```

Номер пломбы и номер партии проиндексированы по `lower(...)`: их ищут руками за стойкой лаборатории, и регистр там никто не соблюдает.

Уникальные частичные индексы там, где уникальность нужна только среди живых записей: инвентарный номер актива, номер контракта, основной контакт клиента, ведущий на заявке.

---

## Миграции

| № | Что добавила |
|---|---|
| 001 | филиалы, пользователи, клиенты, заявки, чек-лист, медиа, отчёты, RLS |
| 002 | финансы: счета, расходы, двойная запись, курсы, агрегат |
| 003 | реквизиты и глава офиса |
| 004 | культуры и порты, контракт/объём на заявке |
| 005 | активы и амортизация |
| 006 | запасной курс валюты до начала истории |
| 007 | организация → страна → офис → отдел |
| 008 | RBAC: права, роли, области видимости; переписаны все политики |
| 009 | журнал аудита (только добавление) |
| 010 | архивирование вместо удаления |
| 011 | контактные лица и контракты |
| 012 | жизненный цикл заявки, история статусов, исполнители, приоритет, версия |
| 013 | инспекции как отдельная сущность: статусы, история, исполнители, замечания, замеры, категории фото |
| 014 | пробы, цепочка ответственного хранения (только на добавление), пломбы, лаборатории |

Откат миграций не предусмотрен: вперёд и только вперёд, как в любой системе, где данные важнее схемы.

### Что миграция 014 намеренно не сделала

**Не создала ни одной пробы из существующих данных.** В базе есть 162 заявки типа «отбор проб» с заполненными чек-листами, но у всех пунктов (`sampling_method`, `seal_numbers`, `incremental_count`, `distribution`) поле `value` пусто — проставлен только результат. Номера пломбы, метода и количества там нет; собрать из этого запись пробы значило бы их выдумать. Эта история остаётся там, где она есть — в чек-листе инспекции, — и пробы начинаются с сегодняшнего дня.

**Не создала ни одной лаборатории.** Ни одно поле в базе не говорит, в каких офисах лаборатория действительно есть (отделов с типом `laboratory` не заведено ни у одного офиса), поэтому таблица создаётся пустой. Лаборатории заводит администратор на экране «Лаборатории» (`org.manage`); демо-сид создаёт свои три и помечен как демо-данные.

Закрытая лаборатория (`is_active = false`) исчезает из списка отправки, но остаётся в базе и на экране администрирования: пробы, уже отправленные туда, продолжают ссылаться на строку, которая по-прежнему говорит, чем она была.
