{
  "name": "@gsi/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types and constants (Job, Report, roles, checklist templates)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
